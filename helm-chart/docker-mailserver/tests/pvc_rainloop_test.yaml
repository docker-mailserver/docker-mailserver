
suite: pvc_rainloop creation
templates:
  - pvc_rainloop.yaml
tests:
  
  - it: should apply annotations from persistence.annotations
    set:
      rainloop.persistence.annotations.backup\.banana\.io/deltas: pancakes
    asserts:
      - equal:
          path: metadata.annotations.backup\.banana\.io/deltas
          value: pancakes

