---
apiVersion: "v1"
kind: "Secret"
metadata:
  labels:
    app: {{ template "dockermailserver.fullname" . }}
    chart: "{{ .Chart.Name }}-{{ .Chart.Version }}"
    heritage: "{{ .Release.Service }}"
    release: "{{ .Release.Name }}"
  name: {{ template "dockermailserver.fullname" . }}
  namespace: {{ template "dockermailserver.fullname" . }}
data:
  # todo - pretty sure this is just a placeholder foo: {{ .Files.Get "files/etc/foo.xml" | b64enc | quote}}
  bar: {{ .Values.Bar | b64enc | quote }}
  opendkim-keys: {{ .Values.Bar | b64enc | quote }}